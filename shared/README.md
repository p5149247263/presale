# Shared Contracts

Shared cross-layer contracts live here.

- `types.ts`: TypeScript data contracts used by frontend.
- Backend source-of-truth schemas are Pydantic models in `backend/app/models/schemas.py`.
